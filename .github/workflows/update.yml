name: 세계 흐름 대시보드 자동 업데이트

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: 체크아웃
      uses: actions/checkout@v3
    
    - name: Python 설정
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 패키지 설치
      run: |
        pip install requests pytz
    
    - name: 대시보드 업데이트
      run: |
        python update_dashboard.py
    
    - name: 변경사항 커밋 및 푸시
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: "🔄 자동 업데이트: ${{ github.run_number }}"
        file_pattern: index.html
