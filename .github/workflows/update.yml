name: 세계 흐름 대시보드 자동 업데이트

on:
  schedule:
    # 매일 한국시간 오전 9시 (UTC 0시)에 실행
    - cron: '0 0 * * *'
  
  # 수동 실행도 가능하게
  workflow_dispatch:

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    
    steps:
    - name: 체크아웃
      uses: actions/checkout@v3
    
    - name: Python 설정
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 패키지 설치
      run: |
        pip install requests pytz feedparser
    
    - name: 대시보드 업데이트
      run: |
        python update_dashboard.py
    
    - name: 변경사항 커밋
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add index.html
        git diff --quiet && git diff --staged --quiet || git commit -m "🔄 자동 업데이트: $(date +'%Y-%m-%d %H:%M KST')"
    
    - name: GitHub에 푸시
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: main
```

### Step 3: 저장

**Commit new file** 클릭!

### Step 4: 확인

파일 목록이 이렇게 보여야 해요:
```
📁 .github
   📁 workflows
      📄 update.yml       ← 이게 생겼으면 성공!
📄 index.html
📄 requirements.txt
📄 update_dashboard.py
